#!/usr/bin/env python
# -*- coding:utf-8 -*-
# 结论: 冒号后面的索引是绝对取不到的,[-1:0]排除了第一个, [-1:]是包含最后一个的,这一点在列表元素只有1个的时候尤为重要
# 另外就是起始索引要是溢出了取到的是空而不会报错
a=[9]
print(a[0:-1])
print(a[-1:0])
print(a[1:])
a=[1,2,3,4,5]
a[::2] = [6,6,6]
print(a)